# typed: false
# frozen_string_literal: true

RSpec.describe Docusearch::Api do
  it "has a version number" do
    expect(Docusearch::Api::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
